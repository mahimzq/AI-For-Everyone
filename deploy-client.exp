#!/usr/bin/expect -f
set password "Mahim@9898@@"
spawn ssh  root@187.124.113.85 "cd /var/www/ai4you.mindsetai.cloud && unzip -o deploy-client.zip && cp -r dist/* public_html/ && rm -rf dist/"
expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "$password\r"
    }
}
expect eof
